/*
Write a function which converts the input string to uppercase.
*/

function convertToUpperCase(str) {
    const word = str.toUpperCase()
    return word;
}
let result = convertToUpperCase('hello')
console.log(result)